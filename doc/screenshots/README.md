# Screenshots

Placeholders referenced from the main [README](../../README.md#widgets). Drop matching PNGs in
here (same filenames) using the example app's "Bundled widgets" tab — `flutter run` inside
`example/`, or `flutter run -d chrome` for the web build — and they'll render automatically.

| Filename | What to capture |
| --- | --- |
| `location_search_field.png` | `LocationSearchField` with a few suggestions showing after typing a partial village name. |
| `location_picker_default.png` | `LocationPicker` in its default 4-level mode (District → Subcounty → Parish → Village), mid-cascade. |
| `location_picker_region.png` | `LocationPicker` with the "Include region / sub-region levels" switch on (6-level cascade), mid-cascade. |

This file itself isn't referenced anywhere - it just keeps the directory tracked in git until the
images land.
